<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Connect to a MariaDB server.
To connect to a MariaDB server you need to provide a connection string that contains all the required
information.
The simple format of the connection string is:

    jdbc:mariadb://[host]/[database]?[properties]

Some examples:

    jdbc:mariadb://localhost:3306/test
    jdbc:mariadb://localhost/test?user=root&amp;password=root

Read more at the: [MariaDB Connection String
Specification](https://mariadb.com/kb/en/mariadb/about-mariadb-connector-j/)
    </description>
    <examples>
        <example title="Usage">
            <header>Connect to a database using credentials</header>
            <code>
                use MariaDB;

                MariaDB.connect(
                    "jdbc:mariadb://localhost/my-database" ::
                    "?user=root" ::
                    "&amp;password=super1secret"
                );

                // Now you can use the MariaDB constructs
                // To interact with the database
            </code>
        </example>
    </examples>
    <references>
        <reference>query</reference>
    </references>
    <searchTags>
        MySQL, mariadb, connection, database
    </searchTags>
</function>