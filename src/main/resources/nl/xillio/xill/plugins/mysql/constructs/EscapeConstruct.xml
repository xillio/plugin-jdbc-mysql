<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Escape a string value for usage in queries.
*Note:* if you use parameters in your queries there is no need to escape them.
    </description>
    <examples>
        <example title="Usage">
            <header>Escape a string</header>
            <code>
                use MySQL as db, System;

                var string = 'Hello, this quote: " might be a problem';
                var escaped = db.escape(string);

                System.print(escaped);
            </code>
        </example>
    </examples>
    <references>
        <reference>query</reference>
    </references>
</function>